# Exercice 012 : Button Counter
# Top module pour le hardware
MODULE = button_counter_top
# Sources : module de base + wrapper top-level
SOURCES = button_counter.v button_counter_top.v
# Testbench teste le module de base (pas le top)
TESTBENCH = button_counter_tb.v

# Inclure le Makefile commun
include ../common.mk

# Exercice 013 : Stopwatch
# Top module pour le hardware
MODULE = stopwatch_top
# Sources : module de base + wrapper top-level
SOURCES = stopwatch.v stopwatch_top.v
# Testbench teste le module de base (pas le top)
TESTBENCH = stopwatch_tb.v

# Inclure le Makefile commun
include ../common.mk
